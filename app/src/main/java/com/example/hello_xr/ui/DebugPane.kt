package com.example.hello_xr.ui

import androidx.compose.runtime.Composable

@Composable
fun DebugPane() {
  /*
  val position = Vector3(0f, 0.5f, -1f) // 1m in front, 0.5m up
  val pose = remember { Pose(position) }

  Subspace {
      SpatialPanel(
          modifier = SubspaceModifier
              .offset(pose)
              .width(300.dp)
      ) {
          Box(
              modifier = Modifier
                  .background(Color.DarkGray.copy(alpha = 0.8f))
                  .padding(16.dp)
          ) {
              Text(
                  text = "Pose: (%.2f, %.2f, %.2f)".format(
                      pose.translation.x,
                      pose.translation.y,
                      pose.translation.z
                  ),
                  color = Color.White,
                  fontSize = 16.sp
              )
          }
      }
  }*/
}
